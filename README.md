# Hello Java Web

一个使用 Java 21、Spring Boot 和 Maven 构建的最小 Web 项目。

## 运行

```bash
mvn spring-boot:run
```

启动后在浏览器打开 <http://localhost:8080/hello>，将看到：

```text
Hello Java Web!
```

## 测试

```bash
mvn test
```
