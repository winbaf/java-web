{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Spring Boot with Maven",
            "type": "shell",
            "command": "mvn",
            "args": [
                "spring-boot:run"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": []
        }
    ]
}
